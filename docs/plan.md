# Plan

> The vision: what this project is and why. The loop reads this for direction; `specs/` carries the
> concrete, prioritized build tracks.

## Goal

_Describe the product in 2-3 sentences. What is being built and for whom._

## Approach

- Deterministic, well-typed packages under `src/`, each behind a clear seam (see
  `docs/INTERFACES.md`).
- Built by the gated Ralph loop, one small change per iteration, 100% covered from day one.

## Milestones

1. _First slice that proves the seam end-to-end._
2. _..._
